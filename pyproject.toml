[project]
name = "chatgpt_scraper"
version = "0.1.0"
description = "A package for ChatGPT"
authors = [{name = "Usama (PSEUDO)", email = "pseudo.usama@gmail.com"}]
dependencies = [
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

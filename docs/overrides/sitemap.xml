<?xml version="1.0" encoding="UTF-8"?>
{#-
    Copy of mkdocs/templates/sitemap.xml with one addition: the landing page.

    content/index.html is a static file, not a MkDocs page, so it never appears
    in `pages` — leaving the site root, the page we most want indexed, out of
    the sitemap entirely. Emit it explicitly ahead of the generated entries.
-#}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    <url>
         <loc>{{ config.site_url|e }}</loc>
    </url>
{%- for file in pages -%}
    {% if not file.page.is_link and (file.page.abs_url or file.page.canonical_url) %}
    <url>
         <loc>{% if file.page.canonical_url %}{{ file.page.canonical_url|e }}{% else %}{{ file.page.abs_url|e }}{% endif %}</loc>
         {% if file.page.update_date %}<lastmod>{{file.page.update_date}}</lastmod>{% endif %}
    </url>
    {%- endif -%}
{% endfor %}
</urlset>
